# BOLD Mortality Prediction Model

A machine learning project for predicting mortality outcomes.

## Getting Started

```bash
pip install -r requirements.txt
python main.py
```

## Project Structure

- `main.py` — entry point
- `requirements.txt` — Python dependencies
